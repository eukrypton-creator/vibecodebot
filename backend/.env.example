ADMIN_KEY=PASTE_A_RANDOM_SECRET_HERE
PORT=3000

# Optional: directory where licenses.json is stored.
# Set this to a mounted Railway volume path (e.g. /app/data) so keys survive redeploys.
# Leave empty to store next to the app (data/ folder, resets on redeploy).
DATA_DIR=
